

/*document.getElementById("portsAvail").addEventListener("click", function(e) {
  if (e.target && e.target.nodeName == "LI") {
    console.log(e.target.innerText + " was clicked");
    socket.emit('query', e.target.innerText);
  }
});*/
